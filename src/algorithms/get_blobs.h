#include <models/object.h>
#include <models/cluster.h>
#include <misc/misc.h>
#include <config.h>

using namespace std;

vector<cluster> get_blobs(vector<cluster> clusters) {
     int length = sizeof(clusters)/sizeof(cluster);
     

};
